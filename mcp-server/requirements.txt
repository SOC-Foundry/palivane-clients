# Pin to the v1 line: v2 renamed FastMCP -> MCPServer and changed APIs. v1's FastMCP is the
# stable, widely-documented surface for Claude Desktop/Code wiring. v2 migration is a follow-up.
mcp>=1.2,<2
httpx>=0.27
